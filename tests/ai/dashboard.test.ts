import { shortest } from "@antiwork/shortest";

shortest("Navegar al dashboard de finanzas y verificar que se muestran los KPI principales (Ventas, Gastos, Margen) y el gráfico de historial.");
shortest("Verificar que al hacer clic en 'Registrar Venta', se abre el formulario y permite introducir datos básicos.");
shortest("Validar que la previsión del modelo 111 está visible en la sección de Impuestos.");
