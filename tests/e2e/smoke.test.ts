import { shortest } from '@antiwork/shortest';

shortest('Verify that the user can login and see the daily sales dashboard');
shortest('Check that the daily sales summary cards are visible');
shortest('Verify that no error messages about data fetching appear on the dashboard');
